{
  "description": "Stop-time review gate for both hosts (Codex and Claude Code). Inactive until `dsh-companion setup --enable-review-gate` runs for the workspace.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/stop-review-gate-hook.mjs\"",
            "timeout": 900
          }
        ]
      }
    ]
  }
}
